#include<bits/stdc++.h>
#define ll long long int
using namespace std;

int main()
{
    ll a, b , c; 
    cin >> a >> b; 
    cout << a << " + " << b << " = " << (a+b) <<'\n';
    cout << a << " * " << b << " = " << (a*b) <<'\n';
    cout << a << " - " << b << " = " << (a-b) <<'\n';
    return 0;
}